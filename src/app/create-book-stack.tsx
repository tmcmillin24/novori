import CreateFlowPlaceholder from '../components/CreateFlowPlaceholder';

export default function CreateBookStackScreen() {
  return (
    <CreateFlowPlaceholder
      icon="albums-outline"
      eyebrow="BOOK STACK"
      title="Build a stack"
      description="Choose multiple books, arrange them your way, name the collection, and save it before deciding whether to share it."
      phaseLabel="Full flow arrives in Phase 4"
    />
  );
}
